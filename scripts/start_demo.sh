#!/usr/bin/env bash
# PC 演示一键启动（开发计划阶段八）。
# mock 模式下起 Web 后端，浏览器即可看到完整系统（识别/抓取/库存全真实流转，硬件为 Mock）。
# 适合答辩前自检、无真机时演示界面。
set -e

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
PORT="${1:-8000}"

echo "============================================================"
echo "  电力库房机械臂管理系统 —— PC 演示模式"
echo "============================================================"
echo "  1) 确认 config/web.yaml 的 mock_mode: true"
echo "  2) 启动 Web 后端：http://127.0.0.1:${PORT}/"
echo "  3) 浏览器点「开始识别抓取」，观察状态/识别/库存实时变化"
echo "------------------------------------------------------------"
echo "  命令行完整演示（可选，另开终端）："
echo "    python scripts/demo_full_system.py"
echo "============================================================"
echo

cd "$ROOT/web/backend"
exec uvicorn app:app --host 0.0.0.0 --port "$PORT" --reload
